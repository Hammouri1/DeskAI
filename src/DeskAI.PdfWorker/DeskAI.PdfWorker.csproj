<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <!-- The App publishes this helper as a separate Windows executable. Keeping the RID on
         the project makes locked solution restore and release publish agree. -->
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="PdfPig" />
  </ItemGroup>
</Project>
