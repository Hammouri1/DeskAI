<Page
    x:Class="DeskAI.App.Views.OrganizePage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:DeskAI.App.ViewModels"
    xmlns:controls="using:DeskAI.App.Controls"
    Background="{ThemeResource DeskGroundBrush}">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
        <StackPanel x:Name="PageContent" Style="{StaticResource PageContentStyle}">

            <!-- One job, said once: what this page is for and the promise that goes with it. -->
            <Border Style="{StaticResource PageIntroStyle}"><StackPanel Spacing="6" MaxWidth="640" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock Style="{StaticResource PageTitleStyle}" Text="Tidy a folder" />
                    <controls:HelpButton Topic="organize.tidy" />
                </StackPanel>
                <TextBlock Style="{StaticResource BodySecondaryStyle}"
                           Text="Pick a folder. DeskAI suggests where things go. Nothing moves until you say so." />
            </StackPanel></Border>

            <!--
                Which folder. The list holds folders already connected; the button adds one. The
                pill beside it states the folder's permission in a word, with the accent only when
                tidying is allowed, because that is the one state that is a permission.
            -->
            <Border Style="{StaticResource CardStyle}"
                    Visibility="{x:Bind ViewModel.HasFolders, Mode=OneWay}">
                <StackPanel Spacing="12">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="12">
                        <ComboBox HorizontalAlignment="Stretch"
                                  PlaceholderText="Pick a folder"
                                  AutomationProperties.Name="Folder to tidy"
                                  Visibility="{x:Bind ViewModel.HasFolders, Mode=OneWay}"
                                  ItemsSource="{x:Bind ViewModel.Folders}"
                                  SelectedItem="{x:Bind ViewModel.SelectedFolder, Mode=TwoWay}" />
                        <Border Grid.Row="1" HorizontalAlignment="Left" Style="{StaticResource AccentPillStyle}"
                                Visibility="{x:Bind ViewModel.CanStopTidying, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontSize="11" Glyph="&#xE73E;" Foreground="{ThemeResource DeskAccentBrush}" />
                                <TextBlock Style="{StaticResource PillTextStyle}" Text="Allowed to tidy" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" HorizontalAlignment="Left" Style="{StaticResource PillStyle}"
                                Visibility="{x:Bind ViewModel.NeedsPermission, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontSize="11" Glyph="&#xE8A5;" Foreground="{ThemeResource DeskTextSecondaryBrush}" />
                                <TextBlock Style="{StaticResource PillTextStyle}" Text="Look only" />
                            </StackPanel>
                        </Border>
                        <Button Grid.Column="1" Content="Choose inside your folders"
                                ToolTipService.ToolTip="Choose Desktop, Downloads, Documents, Pictures, or a folder inside one of them."
                                Click="OnChooseFolderClick" />
                    </Grid>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <ProgressRing Width="18" Height="18" IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />
                        <HyperlinkButton Padding="0" Content="Stop tidying this folder"
                                         Visibility="{x:Bind ViewModel.CanStopTidying, Mode=OneWay}"
                                         Command="{x:Bind ViewModel.StopTidyingCommand}" />
                        <HyperlinkButton Padding="0" Content="Look again"
                                         Visibility="{x:Bind ViewModel.CanStopTidying, Mode=OneWay}"
                                         Command="{x:Bind ViewModel.RefreshCommand}" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource BodySecondaryStyle}"
                               Visibility="{x:Bind ViewModel.HasMessage, Mode=OneWay}"
                               Text="{x:Bind ViewModel.Message, Mode=OneWay}" />
                    <!-- Only when an automatic check's notice opened this folder. -->
                    <TextBlock Style="{StaticResource BodySecondaryStyle}" TextWrapping="Wrap"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Visibility="{x:Bind ViewModel.HasReviewNote, Mode=OneWay}"
                               Text="{x:Bind ViewModel.ReviewNote, Mode=OneWay}" />

                    <!--
                        Tidy while I'm away (V0.9, ADR 0031): the standing yes for this folder. Turning it
                        on opens a dialog naming the rules and the ceiling; off needs none. The line
                        under it says the state, in the caution colour when DeskAI stopped it and why.
                    -->
                    <StackPanel Spacing="4" Margin="0,6,0,0" Visibility="{x:Bind ViewModel.HasAwaySwitch, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <ToggleSwitch x:Name="AwaySwitch"
                                          Header="Tidy this folder while I'm away"
                                          OnContent="On" OffContent="Off"
                                          IsEnabled="{x:Bind ViewModel.CanUseAwaySwitch, Mode=OneWay}"
                                          IsOn="{x:Bind ViewModel.IsAwayOn, Mode=OneWay}"
                                          Toggled="OnAwayToggled"
                                          AutomationProperties.Name="Tidy this folder while I'm away" />
                            <controls:HelpButton VerticalAlignment="Top" Topic="organize.away" />
                        </StackPanel>
                        <TextBlock Style="{StaticResource CaptionStyle}" TextWrapping="Wrap" MaxWidth="560" HorizontalAlignment="Left"
                                   Visibility="{x:Bind ViewModel.AwayLineIsPlain, Mode=OneWay}"
                                   Text="{x:Bind ViewModel.AwayLine, Mode=OneWay}" />
                        <TextBlock Style="{StaticResource CaptionStyle}" TextWrapping="Wrap" MaxWidth="560" HorizontalAlignment="Left"
                                   Foreground="{ThemeResource DeskCautionBrush}"
                                   Visibility="{x:Bind ViewModel.AwayLineIsCaution, Mode=OneWay}"
                                   Text="{x:Bind ViewModel.AwayLine, Mode=OneWay}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!--
                What DeskAI did while nobody was watching. First, above everything else, because
                undo is the primary control for an unattended run: the Undo here is the same Undo
                as any tidy, for the latest run. Lines carry counts and times, never a file name.
            -->
            <Border Style="{StaticResource CardStyle}" Visibility="{x:Bind ViewModel.HasAwayRuns, Mode=OneWay}">
                <StackPanel Spacing="10" MaxWidth="640" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon FontSize="16" Glyph="&#xE823;" Foreground="{ThemeResource DeskTextSecondaryBrush}" />
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="While you were away" />
                    </StackPanel>
                    <ItemsControl ItemsSource="{x:Bind ViewModel.AwayRuns}" AutomationProperties.Name="What DeskAI did while you were away">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <TextBlock Style="{StaticResource BodySecondaryStyle}" TextWrapping="Wrap" Margin="0,0,0,6"
                                           Foreground="{ThemeResource DeskTextPrimaryBrush}" Text="{x:Bind}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Undo the latest run" Click="OnUndoClick"
                                Visibility="{x:Bind ViewModel.CanUndo, Mode=OneWay}" />
                        <Button Content="Got it" Command="{x:Bind ViewModel.GotItCommand}" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource CaptionStyle}" TextWrapping="Wrap"
                               Text="Nothing was deleted. Undo puts back the files from the latest run if they have not changed since." />
                </StackPanel>
            </Border>

            <!--
                A tidy that stopped part-way. It comes first because nothing else happens in
                this folder until it is answered. Caution, not accent: it is a question, not a
                promise. Neither button is accent either; Undo moves files, Keep changes none.
            -->
            <Border Style="{StaticResource CardStyle}" Visibility="{x:Bind ViewModel.HasInterrupted, Mode=OneWay}">
                <StackPanel Spacing="12" MaxWidth="640" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon FontSize="16" Glyph="&#xE7BA;" Foreground="{ThemeResource DeskCautionBrush}" />
                        <TextBlock Style="{StaticResource SectionTitleStyle}" TextWrapping="Wrap"
                                   Text="{x:Bind ViewModel.InterruptedTitle, Mode=OneWay}" />
                        <controls:HelpButton Topic="organize.interrupted" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource BodySecondaryStyle}"
                               Text="{x:Bind ViewModel.InterruptedNote, Mode=OneWay}" />
                    <ItemsControl ItemsSource="{x:Bind ViewModel.InterruptedFiles}"
                                  AutomationProperties.Name="Files to check"
                                  Visibility="{x:Bind ViewModel.HasInterruptedFiles, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:TidyLeftAloneViewModel">
                                <StackPanel Spacing="2" Margin="0,0,0,8">
                                    <TextBlock FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                               Text="{x:Bind FileName}" />
                                    <TextBlock Style="{StaticResource CaptionStyle}" TextWrapping="Wrap"
                                               Foreground="{ThemeResource DeskCautionBrush}"
                                               Text="{x:Bind Reason}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="{x:Bind ViewModel.UndoInterruptedText, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.CanUndoInterrupted, Mode=OneWay}"
                                Click="OnUndoInterruptedClick" />
                        <Button Content="{x:Bind ViewModel.KeepInterruptedText, Mode=OneWay}"
                                Command="{x:Bind ViewModel.KeepInterruptedCommand}" />
                        <ProgressRing Width="18" Height="18" IsActive="{x:Bind ViewModel.IsTidying, Mode=OneWay}" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource BodySecondaryStyle}" TextWrapping="Wrap"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Visibility="{x:Bind ViewModel.HasInterruptedMessage, Mode=OneWay}"
                               Text="{x:Bind ViewModel.InterruptedMessage, Mode=OneWay}" />
                </StackPanel>
            </Border>

            <!-- Nothing connected yet: one clear thing to do. -->
            <Border Style="{StaticResource SoftCardStyle}" Visibility="{x:Bind ViewModel.HasNoFolders, Mode=OneWay}">
                <StackPanel Spacing="10">
                    <TextBlock FontSize="16" FontWeight="SemiBold"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Text="Pick a folder to tidy" />
                    <TextBlock Style="{StaticResource BodySecondaryStyle}" MaxWidth="560" HorizontalAlignment="Left"
                               Text="Downloads and Desktop are good places to start. DeskAI looks first and shows you everything before anything moves." />
                    <Button HorizontalAlignment="Left" Style="{StaticResource AccentButtonStyle}"
                            Content="Choose a folder" Click="OnChooseFolderClick" />
                </StackPanel>
            </Border>

            <!--
                The permission. It wears the green rail because every line in it is a promise
                about what DeskAI will not do, and green means "safe" everywhere in this app.
            -->
            <Border Style="{StaticResource HeroPanelStyle}" Visibility="{x:Bind ViewModel.NeedsPermission, Mode=OneWay}">
                <StackPanel Spacing="14" MaxWidth="600" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Style="{StaticResource SectionTitleStyle}"
                                   Text="{x:Bind ViewModel.PermissionTitle, Mode=OneWay}" />
                        <controls:HelpButton Topic="organize.permission" />
                    </StackPanel>
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon FontSize="13" Glyph="&#xE8B7;" Foreground="{ThemeResource DeskAccentBrush}" />
                            <TextBlock Style="{StaticResource BodySecondaryStyle}" Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                       Text="It moves loose files into folders inside this folder." />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon FontSize="13" Glyph="&#xE72E;" Foreground="{ThemeResource DeskAccentBrush}" />
                            <TextBlock Style="{StaticResource BodySecondaryStyle}" Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                       Text="It never deletes anything and never touches files in subfolders." />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <FontIcon FontSize="13" Glyph="&#xE73E;" Foreground="{ThemeResource DeskAccentBrush}" />
                            <TextBlock Style="{StaticResource BodySecondaryStyle}" Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                       Text="Nothing moves until you press Tidy." />
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Left" Style="{StaticResource AccentButtonStyle}"
                            Content="Allow tidying" Click="OnAllowTidyClick" />
                </StackPanel>
            </Border>

            <!-- What would happen, grouped by where things would go. -->
            <StackPanel Spacing="12" Visibility="{x:Bind ViewModel.HasSuggestions, Mode=OneWay}">
                <StackPanel Spacing="4">
                    <TextBlock Style="{StaticResource SectionTitleStyle}"
                               Text="{x:Bind ViewModel.SummaryTitle, Mode=OneWay}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Style="{StaticResource CaptionStyle}" VerticalAlignment="Center"
                                   Text="{x:Bind ViewModel.SuggestionsFromText, Mode=OneWay}" />
                        <controls:HelpButton Topic="organize.suggestions" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource CaptionStyle}"
                               Foreground="{ThemeResource DeskCautionBrush}"
                               Visibility="{x:Bind ViewModel.HasLimitNote, Mode=OneWay}"
                               Text="{x:Bind ViewModel.LimitNote, Mode=OneWay}" />
                </StackPanel>

                <ItemsControl ItemsSource="{x:Bind ViewModel.Groups}" AutomationProperties.Name="Suggested folders">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:TidyGroupViewModel">
                            <Border Style="{StaticResource RowCardStyle}">
                                <StackPanel Spacing="6">
                                    <Grid ColumnDefinitions="Auto,Auto,*,Auto" ColumnSpacing="12">
                                        <CheckBox IsThreeState="True" MinWidth="0" VerticalAlignment="Center"
                                                  AutomationProperties.Name="{x:Bind DisplayName}"
                                                  IsChecked="{x:Bind IsIncluded, Mode=TwoWay}" />
                                        <FontIcon Grid.Column="1" FontSize="16" Glyph="{x:Bind Glyph}"
                                                  Foreground="{ThemeResource DeskTextSecondaryBrush}" />
                                        <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                                   Text="{x:Bind DisplayName}" />
                                        <Border Grid.Column="3" Style="{StaticResource PillStyle}">
                                            <TextBlock Style="{StaticResource PillTextStyle}" Text="{x:Bind CountText}" />
                                        </Border>
                                    </Grid>
                                    <!-- Only the "AI isn't sure" group has a note: its files wait to be ticked. -->
                                    <TextBlock Style="{StaticResource CaptionStyle}"
                                               Foreground="{ThemeResource DeskCautionBrush}"
                                               Visibility="{x:Bind HasNote}"
                                               Text="{x:Bind Note}" />
                                    <Expander Header="Show files" HorizontalAlignment="Stretch">
                                        <ItemsControl ItemsSource="{x:Bind Items}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="viewmodels:TidyItemViewModel">
                                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" Margin="0,0,0,10">
                                                        <CheckBox MinWidth="0" VerticalAlignment="Top"
                                                                  AutomationProperties.Name="{x:Bind FileName}"
                                                                  IsEnabled="{x:Bind CanBeIncluded}"
                                                                  IsChecked="{x:Bind IsIncluded, Mode=TwoWay}" />
                                                        <StackPanel Grid.Column="1" Spacing="2">
                                                            <TextBlock FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                                                       Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                                                       Text="{x:Bind FileName}" />
                                                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{x:Bind Reason}" />
                                                            <TextBlock Style="{StaticResource CaptionStyle}" Text="{x:Bind Destination}" />
                                                            <StackPanel Spacing="2" Visibility="{x:Bind HasSameName}">
                                                                <TextBlock Style="{StaticResource CaptionStyle}"
                                                                           Foreground="{ThemeResource DeskCautionBrush}"
                                                                           Text="{x:Bind SameNameMessage}" />
                                                                <ToggleSwitch OffContent="Skip this file"
                                                                              OnContent="Keep both (adds a number)"
                                                                              AutomationProperties.Name="Keep both files"
                                                                              IsOn="{x:Bind KeepBoth, Mode=TwoWay}" />
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Expander>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!--
                Asking AI. Nothing here sends anything: the button opens a dialog showing exactly
                what the AI would see, and only Send in that dialog sends it. The button is a
                plain one, because pressing it confirms nothing; the answer appears right below.
            -->
            <Border Style="{StaticResource CardStyle}" Visibility="{x:Bind ViewModel.HasAiPanel, Mode=OneWay}">
                <StackPanel Spacing="12" MaxWidth="640" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon FontSize="16" Glyph="&#xE945;" Foreground="{ThemeResource DeskTextSecondaryBrush}" />
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Ask AI" />
                        <controls:HelpButton Topic="organize.askAi" />
                    </StackPanel>
                    <RadioButtons Header="Which files may AI suggest a place for?"
                                  SelectedIndex="{x:Bind ViewModel.SuggestionModeIndex, Mode=TwoWay}">
                        <RadioButton Content="Only files DeskAI doesn't know" />
                        <RadioButton Content="Every file my rules don't place"
                                     IsEnabled="{x:Bind ViewModel.CanUseAiForEveryFile, Mode=OneWay}" />
                    </RadioButtons>
                    <TextBlock Style="{StaticResource BodySecondaryStyle}"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Text="{x:Bind ViewModel.AskAiPrompt, Mode=OneWay}" />
                    <StackPanel Orientation="Horizontal" Spacing="12"
                                Visibility="{x:Bind ViewModel.HasAskable, Mode=OneWay}">
                        <Button Content="{x:Bind ViewModel.AskAiText, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.CanAskAi, Mode=OneWay}"
                                Click="OnAskAiClick" />
                        <ProgressRing Width="18" Height="18" IsActive="{x:Bind ViewModel.IsAskingAi, Mode=OneWay}" />
                        <Button Content="Stop" Command="{x:Bind ViewModel.StopAiCommand}"
                                Visibility="{x:Bind ViewModel.IsAskingAi, Mode=OneWay}" />
                    </StackPanel>
                    <!--
                        Plan this folder (ADR 0034): the same files and sharing rules as Ask AI, but the
                        AI may also name folders. Plain button: the plan is only a preview until Tidy.
                    -->
                    <Border Height="1" Margin="0,4" Background="{ThemeResource DeskLineBrush}" />
                    <TextBlock Style="{StaticResource BodySecondaryStyle}"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Text="{x:Bind viewmodels:TidyViewModel.PlanAiPrompt}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="{x:Bind viewmodels:TidyViewModel.PlanAiText}"
                                IsEnabled="{x:Bind ViewModel.CanPlanAi, Mode=OneWay}"
                                Click="OnPlanAiClick" />
                        <controls:HelpButton Topic="organize.planAi" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource CaptionStyle}"
                               Text="{x:Bind ViewModel.AiSharingNote, Mode=OneWay}" />
                    <TextBlock Style="{StaticResource BodySecondaryStyle}"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Visibility="{x:Bind ViewModel.HasAiMessage, Mode=OneWay}"
                               Text="{x:Bind ViewModel.AiMessage, Mode=OneWay}" />
                </StackPanel>
            </Border>

            <!-- What DeskAI will not touch, and why. Listed rather than silently dropped. -->
            <Expander HorizontalAlignment="Stretch" Visibility="{x:Bind ViewModel.HasLeftAlone, Mode=OneWay}">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock VerticalAlignment="Center" Text="{x:Bind ViewModel.LeftAloneTitle, Mode=OneWay}" />
                        <controls:HelpButton Topic="organize.leftAlone" />
                    </StackPanel>
                </Expander.Header>
                <ItemsControl ItemsSource="{x:Bind ViewModel.LeftAlone}" AutomationProperties.Name="Files left alone">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:TidyLeftAloneViewModel">
                            <StackPanel Spacing="2" Margin="0,0,0,10">
                                <TextBlock FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                           Text="{x:Bind FileName}" />
                                <TextBlock Style="{StaticResource CaptionStyle}" Text="{x:Bind Reason}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <!--
                The one bold element: how many files are ready, the button, and the honest line
                under it, which says why when the button is off for an unanswered question.
            -->
            <Border Style="{StaticResource CardStyle}" Padding="22,18"
                    Visibility="{x:Bind ViewModel.HasSuggestions, Mode=OneWay}">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="18">
                    <TextBlock Style="{StaticResource MetricStyle}" VerticalAlignment="Center"
                               Text="{x:Bind ViewModel.IncludedCount, Mode=OneWay}" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                        <TextBlock FontWeight="SemiBold" Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                   Text="files ticked" />
                        <TextBlock Style="{StaticResource CaptionStyle}" MaxWidth="420" HorizontalAlignment="Left"
                                   Text="{x:Bind ViewModel.TidyNote, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <ProgressRing Width="18" Height="18" IsActive="{x:Bind ViewModel.IsTidying, Mode=OneWay}" />
                        <Button MinWidth="150"
                                Style="{StaticResource AccentButtonStyle}"
                                Content="{x:Bind ViewModel.TidyButtonText, Mode=OneWay}"
                                Command="{x:Bind ViewModel.TidyCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!--
                What pressing Tidy did, right under the button. Never a plain "done" when anything
                stayed behind: each file that stayed is listed with its reason. Undo is here too.
                After DeskAI was reopened the same card shows the folder's last tidy, with Undo.
            -->
            <Border Style="{StaticResource CardStyle}" Visibility="{x:Bind ViewModel.HasResult, Mode=OneWay}">
                <StackPanel Spacing="10" MaxWidth="640" HorizontalAlignment="Left">
                    <TextBlock FontWeight="SemiBold" TextWrapping="Wrap"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Text="{x:Bind ViewModel.ResultSummary, Mode=OneWay}" />
                    <ItemsControl ItemsSource="{x:Bind ViewModel.ResultSkipped}"
                                  AutomationProperties.Name="Files that stayed where they were"
                                  Visibility="{x:Bind ViewModel.HasResultSkipped, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:TidyLeftAloneViewModel">
                                <StackPanel Spacing="2" Margin="0,0,0,8">
                                    <TextBlock FontWeight="SemiBold" TextTrimming="CharacterEllipsis"
                                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                               Text="{x:Bind FileName}" />
                                    <TextBlock Style="{StaticResource CaptionStyle}"
                                               Foreground="{ThemeResource DeskCautionBrush}"
                                               Text="{x:Bind Reason}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Undo" Click="OnUndoClick"
                                Visibility="{x:Bind ViewModel.CanUndo, Mode=OneWay}" />
                        <controls:HelpButton Topic="organize.undo" />
                    </StackPanel>
                    <TextBlock Style="{StaticResource BodySecondaryStyle}"
                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                               Visibility="{x:Bind ViewModel.HasUndoSummary, Mode=OneWay}"
                               Text="{x:Bind ViewModel.UndoSummary, Mode=OneWay}" />
                </StackPanel>
            </Border>

            <!--
                The optional four-step explanation follows the action. It opens for someone who
                has not allowed tidying anywhere yet and closes once the workflow is familiar.
            -->
            <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Header="How tidying works"
                      IsExpanded="{x:Bind ViewModel.HowItWorksOpen, Mode=TwoWay}">
                <StackPanel Spacing="10" MaxWidth="640" HorizontalAlignment="Left">
                    <ItemsControl ItemsSource="{x:Bind ViewModel.HowItWorksSteps}" AutomationProperties.Name="How tidying works">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" Margin="0,0,0,8">
                                    <FontIcon FontSize="12" Glyph="&#xE76C;" VerticalAlignment="Top" Margin="0,3,0,0"
                                              Foreground="{ThemeResource DeskTextSecondaryBrush}" />
                                    <TextBlock Grid.Column="1" Style="{StaticResource BodySecondaryStyle}" TextWrapping="Wrap"
                                               Foreground="{ThemeResource DeskTextPrimaryBrush}"
                                               Text="{x:Bind}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                        <FontIcon FontSize="13" Glyph="&#xE72E;" VerticalAlignment="Top" Margin="0,3,0,0"
                                  Foreground="{ThemeResource DeskAccentBrush}" />
                        <TextBlock Grid.Column="1" Style="{StaticResource BodySecondaryStyle}" TextWrapping="Wrap"
                                   Text="{x:Bind ViewModel.HowItWorksPromise}" />
                    </Grid>
                </StackPanel>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</Page>
